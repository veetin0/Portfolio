Project screenshots go here.

Reference them from `data/projects.ts`:

    media: [{ src: '/shots/lyvo.png', alt: 'Lyvo route matching view' }]

Any project without a `media` array gets a generated preview instead
(see components/ui/Preview.tsx), so this folder can stay empty.
