Put your CV here as `veeti-nurmikoski-cv.pdf`, then it appears in the Signal
sector automatically. Change the filename in `data/profile.ts` if you prefer
another one.

Before you upload it: remove your home address and phone number. This folder is
served publicly and gets scraped. An email address is enough for anyone who
actually wants to hire you — and it is the one contact detail you can replace
later if it leaks.

Until the file exists the download row hides itself, so the site is safe to
deploy without it.
